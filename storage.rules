rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Nega a leitura e escrita para todos por padṛo
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
